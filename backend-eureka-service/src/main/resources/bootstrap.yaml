spring:
  application:
    name: backend-eureka-service
  cloud:
    config:
      fail-fast: true
      retry:
        max-attempts: 200
        max-interval: 10000
      enabled: true
      uri: http://backend-config-service:8090